stages:
  - build
  - sign
  - sync-public

build-dev:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.23.0-debug
    entrypoint: [""]
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'
  before_script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"auth\":\"$(echo -n "$CI_REGISTRY_USER:$CI_REGISTRY_PASSWORD" | base64 | tr -d '\n')\"}}}" > /kaniko/.docker/config.json
  script:
    # Use --digest-file to capture the exact SHA of the pushed image
    - /kaniko/executor
      --context "$CI_PROJECT_DIR"
      --dockerfile "$CI_PROJECT_DIR/Dockerfile"
      --destination "$CI_REGISTRY_IMAGE:dev"
      --digest-file=image_digest.txt
  artifacts:
    paths:
      - image_digest.txt
    expire_in: 1 hour

sign-dev:
  stage: sign
  image: alpine:3.20
  script:
    - apk add cosign
    - IMAGE_DIGEST=$(cat image_digest.txt)
    - echo "Signing image $CI_REGISTRY_IMAGE@$IMAGE_DIGEST"
    # --yes: skips the legal/interactive prompt
    # --tlog-upload=false: replaces the deprecated --tlog flag to skip Rekor
    - export COSIGN_PASSWORD=$COSIGN_PASSWORD
    - cosign sign --yes --tlog-upload=false --key "$COSIGN_PRIVATE_KEY" "$CI_REGISTRY_IMAGE@$IMAGE_DIGEST"